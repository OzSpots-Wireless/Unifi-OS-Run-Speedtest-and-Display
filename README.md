# Unifi-OS-Run-Speedtest-and-Display
# unifi Gateway UXG-lite dosnt have a speedtest builit in...
# this python speedtest probably isnt accurate at high speeeds...

shows a php page with speedtest button and last run.
ssh into Gateway device from your webserver
run a speedtest and output to the same dir
display it
